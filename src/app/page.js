import Connect from '@/pages/connect';
import Careers from "@/pages/careers";

export default function Home() {
    return (
        <main>
            <Connect/>
            <Careers/>
        </main>
    );
}
